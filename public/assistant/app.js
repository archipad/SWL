const $=s=>document.querySelector(s);const root=$('#app'),engine=window.SWL_ATTACK_ENGINE;let stage=1,attacker=null,defender=null,selectedArmy='p1';
const demo1={listName:'Patrouille impériale · démonstration',faction:'Empire',units:[{name:'Stormtroopers',upgrades:[{name:'HH-12 Stormtrooper'},{name:'Stormtrooper Specialist'},{name:'Targeting Scopes'}]},{name:'Snowtroopers',upgrades:[{name:'T-7 Ion Snowtrooper'}]},{name:'AT-ST',upgrades:[{name:'88i Twin Light Blaster'},{name:'DW-3 Concussion Grenade Launcher'}]}]};
const demo2={listName:'Patrouille rebelle · démonstration',faction:'Rebelles',units:[{name:'Rebel Troopers',upgrades:[{name:'Z-6 Trooper'}]},{name:'Rebel Veterans',upgrades:[{name:'CM-O/93 Trooper'}]},{name:'T-47 Airspeeder',upgrades:[{name:'Ax-108 "Ground Buzzer"'}]}]};
const read=(key,fallback)=>{try{return JSON.parse(localStorage.getItem(key))||fallback}catch{return fallback}};const list1=read('swl.list.p1.v1',demo1),list2=read('swl.list.p2.v1',demo2);const usingDemo=!localStorage.getItem('swl.list.p1.v1')||!localStorage.getItem('swl.list.p2.v1');
const keywords=read('swl.keywords.v1',window.SWL_REFERENCE?.keywords||[]),tags=read('swl.card-tags.v1',window.SWL_REFERENCE?.tags||{}),cardNotes=window.SWL_CARD_NOTES||{},weaponProfiles=window.SWL_REFERENCE?.weapons||{};const norm=s=>(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
const displayName=name=>window.SWL_REFERENCE?.names?.[norm(name)]||name;
const specialImages={'at st':'tr-tt.jpg','at rt':'tl-tt.jpg','at rt flamethrower':'tl-tt-flame-projector.jpg','at st mortar launcher':'tr-tt-mortar-launcher.jpg','cm o 93 trooper':'cm-o93-trooper.jpg','mo dk power harpoon':'mo-dk-power-harpoon.jpg','darth vader the emperor s apprentice':'darth-vader-the-emperors-apprentice.jpg','iden s id10 seeker droid':'idens-id10-seeker-droid.jpg','jyn s se 14 blaster':'jyns-se-14-blaster.jpg','sabine s combat shield':'sabines-combat-shield.jpg','sabine s grapple line':'sabines-grapple-line.jpg','snowtrooper':'snowtrooper-upgrade.jpg','rebel veterans':'rebel-veterans.jpg','rebel troopers':'rebel-troopers.jpg','t 47 airspeeder':'t-47-airspeeder.jpg'};const imageFor=name=>`../cards/${specialImages[norm(name)]||norm(name).replace(/ /g,'-')+'.jpg'}`;
// Valeurs lues sur les fenêtres de conversion imprimées des cartes Unité françaises.
// null = tiret imprimé, donc aucune conversion. applicable:false = carte Compagnon sans fenêtre propre.
// Une absence de clé signifie que la carte n'a pas encore été auditée visuellement.
const profile=(attackSurge,defenseSurge,source)=>({attackSurge,defenseSurge,verified:true,source:`Carte Unité ${source}`});
const combatProfiles={
  '1 4 fd laser cannon team':profile('hit','block','Canon laser 1.4 FD'),
  '74 z speeder bikes':profile('hit','block','Motojets 74-Z'),
  'a a5 speeder truck':profile(null,'block','Camion speeder A-A5'),
  'agent kallus':profile(null,null,'Agent Kallus'),
  'ahsoka tano':profile('crit',null,'Ahsoka Tano'),
  'c 3po':{applicable:false,verified:true,source:'Carte Compagnon C-3PO'},
  'cassian andor':profile('hit','block','Cassian Andor'),
  'chewbacca':profile('crit',null,'Chewbacca'),
  'dark trooper squad':profile(null,null,'Escouade de Dark Troopers'),
  'darth vader dark lord of the sith':profile(null,null,'Dark Vador, Sombre Seigneur des Sith'),
  'darth vader the emperors apprentice':profile(null,null,'Dark Vador, Apprenti de l’Empereur'),
  'dewback rider':profile('hit',null,'Chevaucheur de Dewback'),
  'df 90 mortar trooper':profile(null,null,'Soldat au mortier DF-90'),
  'director orson krennic':profile('hit','block','Directeur Orson Krennic'),
  'e web heavy blaster team':profile('crit',null,'Équipe de blaster lourd E-Web'),
  'fleet troopers':profile('hit','block','Soldats de la flotte'),
  'general veers':profile('crit',null,'Général Veers'),
  'han solo':profile('crit','block','Han Solo'),
  'iden versio':profile('hit',null,'Iden Versio'),
  'idens id10 seeker droid':{applicable:false,verified:true,source:'Carte Compagnon Droïde chercheur ID10 d’Iden'},
  'imperial death troopers':profile('hit','block','Death Troopers impériaux'),
  'imperial special forces inferno squad':profile('hit',null,'Forces spéciales impériales, Escouade Inferno'),
  'imperial special forces':profile('hit',null,'Forces spéciales impériales'),
  'jyn erso':profile('crit','block','Jyn Erso'),
  'k 2so':profile('crit',null,'K-2SO'),
  'laat le patrol transport':profile(null,'block','Transport de patrouille LAAT/le'),
  'lando calrissian':profile('crit','block','Lando Calrissian'),
  'leia organa':profile('crit','block','Leia Organa'),
  'luke skywalker hero of the rebellion':profile('crit',null,'Luke Skywalker, Héros de la Rébellion'),
  'luke skywalker jedi knight':profile('crit',null,'Luke Skywalker, Chevalier Jedi'),
  'major marquand':profile('hit','block','Major Marquand'),
  'mandalorian resistance clan wren':profile('hit','block','Résistance mandalorienne, Clan Wren'),
  'mandalorian resistance':profile('hit','block','Résistance mandalorienne'),
  'mark ii medium blaster trooper':profile('hit','block','Soldat au blaster moyen Mark II'),
  'moff gideon':profile('crit','block','Moff Gideon'),
  'r2 d2':profile('hit','block','R2-D2'),
  'range troopers':profile(null,null,'Range Troopers'),
  'rebel commandos strike team':profile('hit','block','Commandos rebelles, Équipe de tireurs d’élite'),
  'rebel commandos':profile('hit','block','Commandos rebelles'),
  'rebel sleeper cell':profile('hit','block','Cellule dormante rebelle'),
  'rebel troopers':profile(null,'block','Soldats rebelles'),
  'rebel veterans':profile('hit','block','Vétérans rebelles'),
  'sabine wren':profile('crit','block','Sabine Wren'),
  'scout troopers strike team':profile(null,'block','Scout Troopers, Équipe de tireurs d’élite'),
  'scout troopers':profile(null,'block','Scout Troopers'),
  'shoretroopers':profile(null,null,'Shoretroopers'),
  'snowtroopers':profile('hit',null,'Snowtroopers'),
  'stormtrooper heavy gunner squad':profile('hit',null,'Stormtroopers, Escouade d’artilleurs lourds'),
  'stormtrooper riot squad':profile(null,null,'Stormtroopers, Escouade antiémeute'),
  'stormtroopers':profile('hit',null,'Stormtroopers'),
  't 47 airspeeder':profile('crit','block','Airspeeder T-47'),
  'tauntaun riders':profile('crit','block','Cavaliers Tauntaun'),
  'the fifth brother':profile('hit',null,'Cinquième Frère'),
  'the seventh sister':profile('hit',null,'Septième Sœur'),
  'at rt':profile('crit','block','TL-TT'),
  'tl tt':profile('crit','block','TL-TT'),
  'at st':profile(null,'block','TR-TT'),
  'tr tt':profile(null,'block','TR-TT'),
  'tx 225 occupier tank':profile(null,null,'Char d’assaut TX-225 Occupier'),
  'wookiee warriors freedom fighters':profile('hit',null,'Guerriers Wookiees, Combattants de la liberté'),
  'wookiee warriors kashyyyk resistance':profile('hit',null,'Guerriers Wookiees, Résistance de Kashyyyk'),
  'x 34 landspeeder':profile('hit','block','Landspeeder X-34')
};
const armies=[{id:'p1',list:list1},{id:'p2',list:list2}];const entries=armies.flatMap(a=>(a.list.units||[]).map((unit,index)=>({id:`${a.id}:${index}`,army:a.id,label:a.list.listName||a.list.faction||a.id,unit})));
function cardTags(name){return tags[norm(name)]||[]}function allResolved(entry){if(!entry)return[];const cards=[entry.unit.name,...(entry.unit.upgrades||[]).map(u=>u.name)];return cards.flatMap(source=>cardTags(source).map(t=>({tag:t,source,def:keywords.find(k=>k.id===t.keywordId)}))).filter(x=>x.def)}function resolved(entry){const seen=new Set;return allResolved(entry).filter(x=>!seen.has(x.def.id)&&seen.add(x.def.id))}
function definitionsFor(cardName){return cardTags(cardName).map(tag=>({tag,def:keywords.find(k=>k.id===tag.keywordId)})).filter(x=>x.def)}
function noteFor(cardName){return cardNotes[norm(cardName)]}
const diceIcons={attackSurge:['asurge.png','Adrénaline d’attaque'],defenseSurge:['dsurge.png','Adrénaline de défense'],hit:['hit.png','Touche'],block:['block.png','Blocage'],crit:['crit.png','Critique']};
function diceIcon(type,extra=''){const [file,label]=diceIcons[type];return `<img class="dice-symbol ${extra}" src="../icons/dice/${file}" alt="${label}" title="${label}">`}
function renderDiceText(text){return text
  .replace(/\[ADR-ATQ\]|adrénalines? d['’]attaque/gi,()=>diceIcon('attackSurge'))
  .replace(/\[ADR-DEF\]|adrénalines? de défense/gi,()=>diceIcon('defenseSurge'))
  .replace(/\[CRITIQUE\]|critiques?/gi,()=>diceIcon('crit'))
  .replace(/\[TOUCHE\]|touches?/gi,()=>diceIcon('hit'))
  .replace(/\[BLOC\]|blocages?|blocs?/gi,()=>diceIcon('block'))}
function definitionText(item){return renderDiceText((item.def.shortDefinition||item.def.definition||'').replace(/\bX\b/g,item.tag.value??'X'))}
function upgradeGallery(entry){
  const upgrades=entry.unit.upgrades||[];
  if(!upgrades.length)return `<section class="upgrades"><div class="section-heading"><span>AMÉLIORATIONS ÉQUIPÉES</span><b>0</b></div><p class="empty-upgrades">Cette unité n’a aucune amélioration dans la liste importée.</p></section>`;
  return `<section class="upgrades"><div class="section-heading"><span>AMÉLIORATIONS ÉQUIPÉES</span><b>${upgrades.length}</b></div><div class="upgrade-grid">${upgrades.map((up,index)=>{
    const defs=definitionsFor(up.name),note=noteFor(up.name),fr=displayName(up.name);
    const definitions=defs.map(item=>`<div class="upgrade-definition"><strong>${item.def.name}${item.tag.value!=null?' '+item.tag.value:''}</strong><p>${definitionText(item)}</p></div>`).join('');
    const ownEffect=note?`<div class="upgrade-definition card-note"><strong>EFFET DE LA CARTE</strong><p>${renderDiceText(note)}</p></div>`:'';
    const unavailable=!definitions&&!ownEffect?`<p class="unavailable">Aucune définition rattachée dans le référentiel. Consultez le texte de la carte.</p>`:'';
    return `<article class="upgrade-card"><button class="upgrade-visual" data-card-name="${fr.replace(/"/g,'&quot;')}" data-card-image="${imageFor(up.name)}" aria-label="Voir la carte ${fr}"><img src="${imageFor(up.name)}" alt="Carte ${fr}" onerror="this.closest('.upgrade-visual').classList.add('missing')"><span aria-hidden="true">+</span></button><div class="upgrade-copy"><small>AMÉLIORATION ${index+1}</small><h3>${fr}</h3>${definitions}${ownEffect}${unavailable}</div></article>`
  }).join('')}</div></section>`
}
function bindCardViewer(){root.querySelectorAll('.upgrade-visual,.unit-card-zoom').forEach(button=>button.onclick=()=>{const dialog=document.createElement('dialog');dialog.className='card-dialog';dialog.innerHTML=`<button class="dialog-close" aria-label="Fermer">×</button><img src="${button.dataset.cardImage}" alt="${button.dataset.cardName}"><strong>${button.dataset.cardName}</strong>`;document.body.append(dialog);dialog.querySelector('button').onclick=()=>dialog.close();dialog.onclick=e=>{if(e.target===dialog)dialog.close()};dialog.onclose=()=>dialog.remove();dialog.showModal()})}
function progress(){document.querySelectorAll('#progress i').forEach((i,n)=>{i.className=n+1===stage?'active':n+1<stage?'done':''})}function name(e){return e?.unit?.name?displayName(e.unit.name):'Unité'}
function tile(e){return `<button class="unit-tile" data-id="${e.id}"><img src="${imageFor(e.unit.name)}" alt="" onerror="this.style.visibility='hidden'"><span><strong>${displayName(e.unit.name)}</strong><small>${(e.unit.upgrades||[]).map(u=>displayName(u.name)).join(' · ')||'Sans amélioration'}</small></span></button>`}
function armyLabel(army,index){return army.list.listName||army.list.faction||`Joueur ${index+1}`}
function armySelector(){return `<div class="army-switch" role="tablist" aria-label="Choisir une liste">${armies.map((army,index)=>`<button type="button" role="tab" aria-selected="${selectedArmy===army.id}" class="${selectedArmy===army.id?'active':''}" data-army="${army.id}"><small>JOUEUR ${index+1}</small><strong>${armyLabel(army,index)}</strong><span>${(army.list.units||[]).length} unité(s)</span></button>`).join('')}</div>`}
function pick(role){const isDefense=role==='defender',available=isDefense?entries.filter(e=>e.army!==attacker.army):entries.filter(e=>e.army===selectedArmy);root.innerHTML=`<section class="intro"><span class="kicker">ÉTAPE ${stage} SUR 4</span><h1>${isDefense?'Quelle unité est attaquée ?':'Quelle unité jouez-vous ?'}</h1><p>${isDefense?'Seules les unités de l’armée adverse sont proposées.':'Choisissez d’abord la liste, puis touchez la troupe jouée.'}</p>${!isDefense?armySelector():''}${usingDemo?'<p class="notice">Mode démonstration : importez deux listes depuis la page principale pour retrouver vos propres unités ici.</p>':''}</section>${[...new Set(available.map(e=>e.label))].map(label=>`<section><div class="army-title"><b>${label}</b></div><div class="unit-grid">${available.filter(e=>e.label===label).map(tile).join('')}</div></section>`).join('')}`;root.querySelectorAll('.army-switch button').forEach(b=>b.onclick=()=>{selectedArmy=b.dataset.army;pick('attacker')});root.querySelectorAll('.unit-tile').forEach(b=>b.onclick=()=>{const e=entries.find(x=>x.id===b.dataset.id);if(isDefense){defender=e;stage=4;overview(defender,'defense')}else{attacker=e;defender=null;stage=2;overview(attacker,'attack')}});progress()}
function displayImpacts(item){return item.def.displaySections?.length?item.def.displaySections:[item.def.impact]}
function group(items,impact,title){const found=items.filter(x=>displayImpacts(x).includes(impact));return `<section><h2>${title}</h2>${found.length?found.map(x=>`<article class="tag"><strong>${x.def.name}${x.tag.value!=null?' '+x.tag.value:''}</strong><small>${definitionText(x)}</small></article>`).join(''):'<p class="notice">Aucun mot-clé renseigné pour cette rubrique. La carte reste visible pour contrôle.</p>'}</section>`}
function surgeResult(type,value){const source=type==='attack'?diceIcon('attackSurge','large'):diceIcon('defenseSurge','large');const result=value==='hit'?diceIcon('hit','large'):value==='crit'?diceIcon('crit','large'):value==='block'?diceIcon('block','large'):'<i class="no-surge" title="Aucune conversion">—</i>';return `<strong class="surge-line">${source}<b>→</b>${result}</strong>`}
function surgePanel(entry){const profile=combatProfiles[norm(entry.unit.name)];if(!profile)return `<div class="surge-panel unknown"><span>CONVERSIONS D’ADRÉNALINE</span><strong>À vérifier sur la carte</strong><small>Aucune valeur n’est inventée tant que le visuel n’a pas été contrôlé.</small></div>`;if(profile.applicable===false)return `<div class="surge-panel unknown"><span>CONVERSIONS D’ADRÉNALINE</span><strong>Profil de l’unité principale</strong><small>${profile.source} · aucune fenêtre de conversion propre n’est imprimée.</small></div>`;return `<div class="surge-panel"><span>CONVERSIONS D’ADRÉNALINE</span>${surgeResult('attack',profile.attackSurge)}${surgeResult('defense',profile.defenseSurge)}<small>Vérifié visuellement · ${profile.source}</small></div>`}
function unitVisual(entry,side=''){const fr=name(entry),img=imageFor(entry.unit.name);return `<button class="unit-card-zoom ${side}" data-card-name="${fr.replace(/"/g,'&quot;')}" data-card-image="${img}" aria-label="Voir la carte ${fr}"><img src="${img}" alt="Carte ${fr}" onerror="this.hidden=true"><span aria-hidden="true">+</span></button>`}
function overview(entry,role){const items=resolved(entry),fr=displayName(entry.unit.name);root.innerHTML=`${role==='defense'?`<div class="duel"><strong>${name(attacker)}</strong><span>ATTAQUE</span><strong>${name(defender)}</strong></div>`:''}<section class="overview ${role}"><div class="hero"><div><span>${role==='attack'?'UNITÉ JOUÉE':'UNITÉ CIBLÉE'}</span><h1>${fr}</h1><p>${entry.label}</p><p>${(entry.unit.upgrades||[]).map(u=>displayName(u.name)).join(' · ')||'Sans amélioration'}</p>${surgePanel(entry)}</div>${unitVisual(entry,role)}</div>${upgradeGallery(entry)}<div class="columns">${group(items,'autre','Déplacement & activation')}${group(items,'attaque','Attaque')}${group(items,'défense','Défense')}</div></section><div class="actions"><button class="secondary" id="back">${role==='attack'?'Changer d’unité':'Changer de cible'}</button><button class="primary" id="next">${role==='attack'?'Choisir la cible →':'Résoudre l’attaque →'}</button></div>`;bindCardViewer();$('#back').onclick=()=>{stage=role==='attack'?1:3;pick(role==='attack'?'attacker':'defender')};$('#next').onclick=()=>{if(role==='attack'){stage=3;pick('defender')}else initAttack()};progress()}
let attackStep=0,attackState=null;
const attackSteps=['Armes & portée','Jet & relances','Modifications','Couvert & esquive','Défense & bilan'];
function profileFor(card){return weaponProfiles[norm(card)]}
function initAttack(){attackStep=0;attackState={range:null,selected:{},counts:{},moved:false,ionEligible:false,activeShields:0,aims:0,lethalAims:0,rerolled:0,roll:{hit:0,crit:0,surge:0,blank:0},cover:'none',coverBlock:0,coverSurge:0,dodges:0,impact:null,armor:null,defense:{block:0,surge:0,blank:0},pierce:null,mixedWarningDismissed:false};resolveScreen()}
function selectedWeaponRows(){const cards=[attacker.unit.name,...(attacker.unit.upgrades||[]).map(u=>u.name)];return cards.flatMap(card=>(profileFor(card)?.weapons||[]).map((weapon,index)=>({card,weapon,index,key:`${norm(card)}:${index}`}))).filter(x=>attackState.selected[x.key])}
function weaponHasKeyword(row,id){const profile=profileFor(row.card),defs=definitionsFor(row.card);return profile?.weapons?.length===1&&defs.some(x=>x.def.category==='arme'&&x.def.id===id)}
function pool(){const downgraded=new Set(attackState.moved?selectedWeaponRows().filter(row=>weaponHasKeyword(row,'encombrant')).map(row=>row.key):[]);return engine.buildPool(selectedWeaponRows(),attackState.counts,downgraded)}
function dieBadge(color,count){return count?`<span class="dice-badge dice-badge-${color}" title="${count} dé(s) ${color}"><span>${count}</span></span>`:''}
function weaponDiceView(dice,count=1){return dice==='variable'?'<span class="variable-dice">Réserve variable</span>':`<span class="dice-row">${dice.map(d=>dieBadge(d.color,d.count*count)).join('')}</span>`}
function poolView(){const p=pool();return `<div class="dice-pool"><span>DÉS À LANCER</span><div class="dice-row dice-row-big">${dieBadge('rouge',p.rouge)}${dieBadge('noir',p.noir)}${dieBadge('blanc',p.blanc)}${p.variable?'<strong>+ réserve variable : voir la carte</strong>':''}</div></div>`}
function activeAttackTags(){const selected=new Set(selectedWeaponRows().map(x=>norm(x.card)));return allResolved(attacker).filter(x=>displayImpacts(x).includes('attaque')&&(x.source===attacker.unit.name||selected.has(norm(x.source))||(profileFor(x.source)?.weapons||[]).length===0))}
function relevantKeywords(){const seen=new Set;return [...activeAttackTags(),...allResolved(defender).filter(x=>displayImpacts(x).includes('défense'))].filter(x=>!seen.has(`${x.def.id}:${x.source}`)&&seen.add(`${x.def.id}:${x.source}`))}
function rulesPanel(){const rows=relevantKeywords();return `<details class="rules-panel" open><summary>Règles qui interviennent (${rows.length})</summary>${rows.length?rows.map(x=>`<article><b>${x.def.name}${x.tag.value!=null?' '+x.tag.value:''}</b><small>${definitionText(x)} · ${displayName(x.source)}</small></article>`).join(''):'<p>Aucun mot-clé d’attaque ou de défense rattaché.</p>'}</details>`}
function sideSurge(entry,side){const p=combatProfiles[norm(entry.unit.name)],type=side==='attack'?'attack':'defense',value=type==='attack'?p?.attackSurge:p?.defenseSurge,label=type==='attack'?'ADRÉNALINE D’ATTAQUE':'ADRÉNALINE DE DÉFENSE';return `<div class="side-surge"><small>${label}</small>${surgeResult(type,value)}<em>${value==='hit'?'vers Touche':value==='crit'?'vers Critique':value==='block'?'vers Blocage':'aucune conversion'}</em></div>`}
function combatAside(entry,side){return `<aside class="combat-side ${side}">${unitVisual(entry,side)}<h3>${side==='attack'?'ATTAQUANT':'DÉFENSEUR'}</h3><strong>${name(entry)}</strong>${sideSurge(entry,side)}<div class="mini-upgrades">${(entry.unit.upgrades||[]).map(u=>`<button class="upgrade-visual" data-card-name="${displayName(u.name).replace(/"/g,'&quot;')}" data-card-image="${imageFor(u.name)}" aria-label="Voir la carte ${displayName(u.name).replace(/"/g,'&quot;')}"><img src="${imageFor(u.name)}" alt=""><span aria-hidden="true">+</span></button>`).join('')||'<small>Sans amélioration</small>'}</div></aside>`}
function numberField(id,label,value,max='99'){const current=Math.max(0,Number(value)||0),limit=Math.max(0,Number(max)||0);return `<div class="quick-field"><span class="quick-label">${label}</span><div class="touch-counter"><button type="button" data-adjust="${id}" data-delta="-1" aria-label="Diminuer" ${current<=0?'disabled':''}>−</button><input id="${id}" aria-label="${id}" type="number" inputmode="numeric" min="0" max="${limit}" value="${current}"><button type="button" data-adjust="${id}" data-delta="1" aria-label="Augmenter" ${current>=limit?'disabled':''}>+</button></div></div>`}
function attackType(){const rows=selectedWeaponRows(),melee=rows.some(x=>x.weapon.range==='melee'),ranged=rows.some(x=>x.weapon.range!=='melee');return melee&&ranged?'mixed':melee?'melee':'ranged'}
function weaponScreen(){const cards=[attacker.unit.name,...(attacker.unit.upgrades||[]).map(u=>u.name)],rows=cards.flatMap(card=>(profileFor(card)?.weapons||[]).map((weapon,index)=>({card,weapon,index,key:`${norm(card)}:${index}`}))),ranges=engine.rangeOptions(rows.map(r=>r.weapon)),mixed=attackType()==='mixed',cumbersome=selectedWeaponRows().some(row=>weaponHasKeyword(row,'encombrant'));return `${poolView()}<h2>1. Portée et réserve d’attaque</h2><p class="step-help">Indiquez la portée mesurée, puis choisissez uniquement les armes et figurines qui contribuent à cette réserve.</p><div class="range-picker">${ranges.map(range=>`<button data-range="${range}" class="${String(attackState.range)===String(range)?'on':''}">${range==='melee'?'CORPS-À-CORPS':`PORTÉE ${range}`}</button>`).join('')}</div>${cumbersome?`<label class="situation-check"><input id="moved" type="checkbox" ${attackState.moved?'checked':''}> L’unité s’est déplacée pendant cette activation <small>Encombrant : les dés de cette arme seront automatiquement dégradés.</small></label>`:''}${mixed?'<div class="combat-warning" role="alert"><span aria-hidden="true">⚠</span><div><strong>RÉSERVE D’ATTAQUE INVALIDE</strong><p>Séparez les armes de corps-à-corps et les armes à distance.</p></div></div>':''}<div class="weapon-picker">${rows.map(r=>{const eligible=engine.weaponEligible(r.weapon.range,attackState.range),on=!!attackState.selected[r.key],count=attackState.counts[r.key]??1;return `<article class="weapon-choice ${on?'on':''} ${eligible?'':'disabled'}"><button class="weapon-toggle" data-key="${r.key}" ${eligible?'':'disabled'}><span class="weapon-check">${on?'✓':''}</span><span class="weapon-copy"><b>${r.weapon.name}</b><small>${displayName(r.card)} · portée ${r.weapon.range==='melee'?'corps-à-corps':r.weapon.range||'à vérifier'}${eligible?'':' · hors portée'}</small></span><span class="weapon-printed-dice">${weaponDiceView(r.weapon.dice)}</span></button><div class="count-control"><button data-minus="${r.key}" ${eligible?'':'disabled'}>−</button><label>Figurines <input data-count="${r.key}" type="number" min="1" max="20" value="${count}" ${eligible?'':'disabled'}></label><button data-plus="${r.key}" ${eligible?'':'disabled'}>+</button></div></article>`}).join('')}</div>${rulesPanel()}`}
function rollScreen(){const r=attackState.roll,p=combatProfiles[norm(attacker.unit.name)],precise=attackKeywordValue('precis-x'),capacity=engine.rerollCapacity(attackState.aims,precise),lethalX=attackKeywordValue('letal-x'),lethal=engine.applyLethal(attackKeywordValue('perforant-x'),lethalX,attackState.lethalAims),critical=attackKeywordValue('critique-x'),converted=engine.convertAttack(r,p?.attackSurge,critical);return `${poolView()}<h2>2. Lancer, relancer et convertir</h2><p class="step-help">Saisissez le résultat final du jet. Les pions Viser consacrés à Létal sont séparés de ceux utilisés pour relancer les dés.</p><div class="result-entry">${numberField('aims','Pions Viser dépensés pour relancer',attackState.aims,20)}${lethalX?numberField('lethalAims','Pions Viser dépensés pour Létal',attackState.lethalAims,lethalX):''}${numberField('rerolled','Dés effectivement relancés',attackState.rerolled,capacity)}${numberField('rollHit',`${diceIcon('hit')} Touches`,r.hit)}${numberField('rollCrit',`${diceIcon('crit')} Critiques`,r.crit)}${numberField('rollSurge',`${diceIcon('attackSurge')} Adrénalines`,r.surge)}${numberField('rollBlank','Vierges',r.blank)}</div><div class="automation-card"><strong>Relances autorisées : ${capacity}</strong><small>${attackState.aims} Viser × (2 + Précis ${precise})</small>${lethalX?`<strong>Létal ${lethalX} : Perforant total ${lethal.pierce}</strong><small>${lethal.lethalUsed} pion(s) Viser converti(s) en Perforant ; ils ne donnent aucune relance.</small>`:''}<strong>Conversion automatique : ${converted.hit} touches · ${converted.crit} critiques</strong><small>Critique ${critical} utilisé : ${converted.criticalUsed}</small></div><div class="conversion-note">Conversion : ${surgeResult('attack',p?.attackSurge)}</div>${rulesPanel()}`}
function attackResults(){const p=combatProfiles[norm(attacker.unit.name)];return engine.convertAttack(attackState.roll,p?.attackSurge,attackKeywordValue('critique-x'))}
function keywordValue(entry,id){return allResolved(entry).filter(x=>x.def.id===id).reduce((n,x)=>n+(Number(x.tag.value)||0),0)}
function attackKeywordValue(id){return activeAttackTags().filter(x=>x.def.id===id).reduce((n,x)=>n+(Number(x.tag.value)||0),0)}
function armorContext(){const tag=allResolved(defender).find(x=>x.def.id==='armure-x');return {tag,hasArmor:!!tag,unlimited:!!tag&&tag.tag.value==null,value:Number(tag?.tag.value)||0}}
function modifiedResults(){const a=attackResults(),armor=armorContext(),impactX=attackKeywordValue('impact-x');if(attackState.impact==null)attackState.impact=armor.hasArmor?Math.min(a.hit,impactX):0;const hitsAfterImpact=a.hit-Math.min(a.hit,attackState.impact||0);if(attackState.armor==null)attackState.armor=armor.hasArmor?Math.min(hitsAfterImpact,armor.unlimited?hitsAfterImpact:armor.value):0;return engine.applyImpactArmor(a,{hasArmor:armor.hasArmor,impactX,impactUsed:attackState.impact,armorUnlimited:armor.unlimited,armorX:armor.value,armorCancelled:attackState.armor})}
function modifierScreen(){const a=attackResults(),m=modifiedResults(),armor=armorContext(),impactX=attackKeywordValue('impact-x'),ionX=attackKeywordValue('ion-x'),flipped=Math.min(ionX,a.hit+a.crit,attackState.activeShields);return `<div class="result-strip"><b>${diceIcon('hit','large')} ${a.hit}</b><b>${diceIcon('crit','large')} ${a.crit}</b></div><h2>3. Modifier les dés d’attaque</h2><p class="step-help">Impact est proposé automatiquement seulement contre Armure. L’annulation d’Armure est ensuite limitée à la valeur imprimée.</p><div class="automation-card"><strong>Impact disponible : ${impactX}</strong><small>Défenseur : ${armor.hasArmor?(armor.unlimited?'Armure':'Armure '+armor.value):'sans Armure'}</small>${ionX?`<strong>Ion ${ionX} : ${flipped} bouclier(s) retourné(s)</strong><small>Confirmez si la cible est un véhicule ou un soldat droïde pour l’attribution finale.</small>`:''}</div>${ionX?`<label class="situation-check"><input id="ionEligible" type="checkbox" ${attackState.ionEligible?'checked':''}> La cible est un véhicule ou un soldat droïde</label><div class="result-entry">${numberField('activeShields','Boucliers actifs avant Ion',attackState.activeShields,20)}</div>`:''}<div class="result-entry">${numberField('impact','Impact utilisé',attackState.impact,Math.min(a.hit,impactX))}${numberField('armor','Touches annulées par Armure',attackState.armor,a.hit)}</div><div class="result-strip"><b>APRÈS MODIFICATIONS</b><b>${diceIcon('hit','large')} ${m.hit}</b><b>${diceIcon('crit','large')} ${m.crit}</b></div>${rulesPanel()}`}
function coverContext(){const blast=activeAttackTags().some(x=>x.def.id==='deflagration'),immuneBlast=allResolved(defender).some(x=>x.def.id==='immunite-deflagration'),sharpshooter=attackKeywordValue('tireur-delite-x'),highVelocity=selectedWeaponRows().length>0&&selectedWeaponRows().every(row=>weaponHasKeyword(row,'haute-velocite'));return {blast,immuneBlast,sharpshooter,highVelocity,effective:engine.effectiveCover(attackState.cover,sharpshooter,blast,immuneBlast)}}
function afterCover(){const a=modifiedResults(),ctx=coverContext();return engine.applyCover(a,{melee:attackType()==='melee',cover:ctx.effective,coverBlock:attackState.coverBlock,coverSurge:attackState.coverSurge,dodges:ctx.highVelocity?0:attackState.dodges})}
function coverScreen(){const a=modifiedResults(),c=afterCover(),ctx=coverContext();return `<div class="result-strip"><b>${diceIcon('hit','large')} ${a.hit}</b><b>${diceIcon('crit','large')} ${a.crit}</b></div><h2>4. Appliquer esquives et couvert</h2><p class="step-help">À distance seulement, saisissez le couvert déterminé sur la table. Tireur d’élite et Déflagration calculent automatiquement le couvert réellement utilisable.</p><div class="cover-options"><button data-cover="none" class="${attackState.cover==='none'?'on':''}">Aucun</button><button data-cover="light" class="${attackState.cover==='light'?'on':''}">Léger</button><button data-cover="heavy" class="${attackState.cover==='heavy'?'on':''}">Lourd</button></div><div class="automation-card"><strong>Couvert effectif : ${ctx.effective==='heavy'?'lourd':ctx.effective==='light'?'léger':'aucun'}</strong><small>${ctx.blast&&!ctx.immuneBlast?'Déflagration ignore le couvert.':ctx.sharpshooter?`Tireur d’élite ${ctx.sharpshooter} réduit le couvert.`:'Aucune réduction automatique.'}</small>${ctx.highVelocity?'<strong>Haute vélocité : esquives interdites</strong>':''}</div><div class="result-entry">${numberField('coverBlock',`${diceIcon('block')} obtenus sur dés de couvert`,attackState.coverBlock,a.hit)}${numberField('coverSurge',`${diceIcon('defenseSurge')} obtenues sur dés de couvert`,attackState.coverSurge,a.hit)}${numberField('dodges','Pions Esquive dépensés',ctx.highVelocity?0:attackState.dodges,ctx.highVelocity?0:a.hit)}</div><div class="result-strip"><b>À DÉFENDRE</b><b>${diceIcon('hit','large')} ${c.hit}</b><b>${diceIcon('crit','large')} ${c.crit}</b></div>${rulesPanel()}`}
function defenseScreen(){const a=afterCover(),d=attackState.defense,p=combatProfiles[norm(defender.unit.name)],lethal=engine.applyLethal(attackKeywordValue('perforant-x'),attackKeywordValue('letal-x'),attackState.lethalAims),pierceX=lethal.pierce,pierceImmune=allResolved(defender).some(x=>x.def.id==='immunite-perforant'||(x.def.id==='immunite-perforant-corps-a-corps'&&attackType()==='melee'));const converted=d.block+(p?.defenseSurge==='block'?d.surge:0);if(attackState.pierce==null)attackState.pierce=pierceImmune?0:Math.min(converted,pierceX);const result=engine.applyDefense(a,d,{defenseSurge:p?.defenseSurge,pierceX,pierceUsed:attackState.pierce,pierceImmune}),ranged=attackType()==='ranged',hadResult=attackResults().hit+attackResults().crit>0,suppressive=activeAttackTags().some(x=>x.def.id==='suppressif'),suppression=ranged&&hadResult?1+(suppressive?1:0):0,ionX=attackKeywordValue('ion-x'),ion=result.wounds>0&&attackState.ionEligible?ionX:0;return `<div class="result-strip"><b>À DÉFENDRE</b><b>${diceIcon('hit','large')} ${a.hit}</b><b>${diceIcon('crit','large')} ${a.crit}</b></div><h2>5. Défense et bilan</h2><p class="step-help">Lancez ${a.hit+a.crit} dé(s) de défense. La conversion et Perforant sont appliqués automatiquement, avec respect d’Immunité : Perforant.</p><div class="result-entry">${numberField('defBlock',`${diceIcon('block')} Blocages`,d.block,a.hit+a.crit)}${numberField('defSurge',`${diceIcon('defenseSurge')} Adrénalines`,d.surge,a.hit+a.crit)}${numberField('defBlank','Vierges',d.blank,a.hit+a.crit)}${numberField('pierce','Perforant utilisé',attackState.pierce,Math.min(result.converted,pierceX))}</div><div class="conversion-note">Conversion : ${surgeResult('defense',p?.defenseSurge)}<small>${pierceImmune?'Immunité : Perforant active.':`Perforant disponible : ${pierceX}${lethal.lethalUsed?` (dont ${lethal.lethalUsed} obtenu par Létal)`:''}.`}</small></div><div class="resolution-log"><strong>JOURNAL DE RÉSOLUTION</strong><ol><li>Après conversion : ${attackResults().hit} touches · ${attackResults().crit} critiques</li><li>Impact ${modifiedResults().impactUsed}, Armure ${modifiedResults().armorCancelled} : ${modifiedResults().hit} touches · ${modifiedResults().crit} critiques</li><li>Couvert ${a.coverCancelled}, Esquive ${a.dodgesUsed} : ${a.hit} touches · ${a.crit} critiques</li><li>${result.converted} blocages convertis, Perforant ${result.pierceUsed} : ${result.blocks} blocages conservés</li></ol></div><div class="total"><span>BLESSURES À APPLIQUER</span><strong>${result.wounds}</strong><small>Suppression à attribuer : ${suppression}. Le défenseur vaincu n’en reçoit pas.</small>${ionX?`<small>Pions Ionique à attribuer : ${ion}${attackState.ionEligible?'':' (cible non confirmée véhicule/droïde)'}</small>`:''}</div>${rulesPanel()}`}
function showMixedWarning(){if(attackType()!=='mixed'||attackState.mixedWarningDismissed)return;const dialog=document.createElement('dialog');dialog.className='warning-dialog';dialog.innerHTML='<div class="warning-icon" aria-hidden="true">⚠</div><strong>Réserve d’attaque invalide</strong><p>Une arme de corps-à-corps ne peut pas être ajoutée à la même réserve qu’une arme à distance.</p><button type="button">J’ai compris</button>';document.body.append(dialog);dialog.oncancel=e=>e.preventDefault();dialog.querySelector('button').onclick=()=>dialog.close();dialog.onclose=()=>{attackState.mixedWarningDismissed=true;dialog.remove()};dialog.showModal()}
function stepIssue(){if(attackStep===0){if(attackState.range==null)return 'Sélectionnez la portée mesurée.';if(!selectedWeaponRows().length)return 'Sélectionnez au moins une arme éligible.';if(attackType()==='mixed')return 'Séparez les armes de corps-à-corps et les armes à distance.'}if(attackStep===1){const p=pool(),rolled=Object.values(attackState.roll).reduce((n,v)=>n+(Number(v)||0),0),expected=p.rouge+p.noir+p.blanc;if(!p.variable&&rolled!==expected)return `Le jet saisi contient ${rolled} dés, mais la réserve en contient ${expected}.`;const max=engine.rerollCapacity(attackState.aims,attackKeywordValue('precis-x'));if(attackState.rerolled>max)return `Précis et les pions Viser autorisent au maximum ${max} relances.`}if(attackStep===4){const a=afterCover(),rolled=Object.values(attackState.defense).reduce((n,v)=>n+(Number(v)||0),0),expected=a.hit+a.crit;if(rolled!==expected)return `Saisissez exactement ${expected} résultats de défense.`}return null}
function resolveScreen(){stage=4+attackStep;const bodies=[weaponScreen,rollScreen,modifierScreen,coverScreen,defenseScreen],issue=stepIssue();root.innerHTML=`<div class="duel"><strong>${name(attacker)}</strong><span>${attackSteps[attackStep]}</span><strong>${name(defender)}</strong></div><section class="attack-workspace">${combatAside(attacker,'attack')}<section class="resolve-center"><div class="attack-stepper">${attackSteps.map((s,i)=>`<button data-go="${i}" class="${i===attackStep?'active':i<attackStep?'done':''}"><b>${i+1}</b><span>${s}</span></button>`).join('')}</div>${issue?`<div class="strict-warning" role="alert">⚠ ${issue}</div>`:''}${bodies[attackStep]()}</section>${combatAside(defender,'defense')}</section><div class="actions"><button class="secondary" id="prev">${attackStep?'← Étape précédente':'← Revoir la cible'}</button><button class="primary" id="nextAttack">${attackStep===4?'Nouvelle attaque':'Étape suivante →'}</button></div>`;bindCardViewer();bindAttackInputs();if(attackStep===0)showMixedWarning();$('#prev').onclick=()=>{if(attackStep){attackStep--;resolveScreen()}else overview(defender,'defense')};$('#nextAttack').onclick=()=>{if(stepIssue()){resolveScreen();return}if(attackStep<4){attackStep++;resolveScreen()}else{attacker=null;defender=null;stage=1;pick('attacker')}};root.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>{if(+b.dataset.go<=attackStep||!stepIssue()){attackStep=+b.dataset.go;resolveScreen()}});progress()}
function bindAttackInputs(){root.querySelectorAll('[data-range]').forEach(b=>b.onclick=()=>{attackState.range=b.dataset.range==='melee'?'melee':+b.dataset.range;attackState.selected={};attackState.impact=null;attackState.armor=null;resolveScreen()});root.querySelectorAll('[data-key]').forEach(b=>b.onclick=()=>{attackState.selected[b.dataset.key]=!attackState.selected[b.dataset.key];attackState.impact=null;attackState.armor=null;if(attackType()!=='mixed')attackState.mixedWarningDismissed=false;resolveScreen()});root.querySelectorAll('[data-count]').forEach(i=>i.oninput=()=>{attackState.counts[i.dataset.count]=Math.max(1,+i.value||1)});root.querySelectorAll('[data-minus]').forEach(b=>b.onclick=()=>{const k=b.dataset.minus;attackState.counts[k]=Math.max(1,(attackState.counts[k]??1)-1);resolveScreen()});root.querySelectorAll('[data-plus]').forEach(b=>b.onclick=()=>{const k=b.dataset.plus;attackState.counts[k]=(attackState.counts[k]??1)+1;resolveScreen()});root.querySelectorAll('[data-cover]').forEach(b=>b.onclick=()=>{attackState.cover=b.dataset.cover;resolveScreen()});['moved','ionEligible'].forEach(id=>{const el=$('#'+id);if(el)el.onchange=()=>{attackState[id]=el.checked;resolveScreen()}});const map={rollHit:['roll','hit'],rollCrit:['roll','crit'],rollSurge:['roll','surge'],rollBlank:['roll','blank'],defBlock:['defense','block'],defSurge:['defense','surge'],defBlank:['defense','blank']};Object.entries(map).forEach(([id,[group,key]])=>{const el=$('#'+id);if(el){el.oninput=()=>{attackState[group][key]=+el.value||0};if(group==='defense')el.onchange=resolveScreen}});['aims','rerolled','coverBlock','coverSurge','dodges','impact','armor','pierce','activeShields'].forEach(id=>{const el=$('#'+id);if(el){el.oninput=()=>{attackState[id]=+el.value||0};if(id==='pierce'||id==='activeShields')el.onchange=resolveScreen}});root.querySelectorAll('[data-adjust]').forEach(button=>button.onclick=()=>{const input=$('#'+button.dataset.adjust),min=Number(input.min)||0,max=Number(input.max)||99,next=Math.max(min,Math.min(max,(Number(input.value)||0)+Number(button.dataset.delta)));input.value=next;input.dispatchEvent(new Event('input',{bubbles:true}));input.dispatchEvent(new Event('change',{bubbles:true}));if(document.body.contains(button)){const counter=button.closest('.touch-counter');counter.querySelector('[data-delta="-1"]').disabled=next<=min;counter.querySelector('[data-delta="1"]').disabled=next>=max}})}
const bindAttackInputsBase=bindAttackInputs;
bindAttackInputs=function(){bindAttackInputsBase();const lethal=$('#lethalAims');if(lethal){lethal.oninput=()=>{attackState.lethalAims=+lethal.value||0;attackState.pierce=null};lethal.onchange=resolveScreen}}
$('#restart').onclick=()=>{attacker=null;defender=null;selectedArmy='p1';stage=1;pick('attacker')};pick('attacker');
